# video-steganography-java
Hide and retrieve secret messages inside video files using LSB (Least Significant Bit) substitution. Built with Java and OpenCV — encodes messages across randomly selected frames using a seeded key stored in the video itself.
